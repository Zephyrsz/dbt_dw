version: 2

sources:
  - name: raw
    meta:
      external_location: "read_csv_auto('./data/{name}.csv')"
    tables:
      - name: project_user_counts
      # Add tables here as you add CSVs to the data/ folder
      # - name: orders
      # - name: customers
      # - name: products
